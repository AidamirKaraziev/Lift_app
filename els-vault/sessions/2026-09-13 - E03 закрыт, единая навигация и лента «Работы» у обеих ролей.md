---
tags: [session, навигация, лента работ, E03]
date: 2026-09-13
---

# 2026-09-13 - E03 закрыт: единая навигация и лента «Работы» у обеих ролей

Эпик E03 «Единая навигация» закрыт 9/9 (S01–S08 и S03b) за три дня,
коммиты `92536a9..a8cfdc9` в ветке `fix/apk-api-origin`. Учёт по этапам —
`.claude/ledger.jsonl`, план — `.claude/plan/E03-edinaya-navigatsiya.md`.

## Что появилось

- **Один бургер** на админа и прораба по `enum AppSection`, разделы
  выбираются адресом (`/objects`, `/works`, …) на штатном `Router`.
- **Экран «Работы»**: заявки и акты ТО одной лентой `GET /work/feed`,
  чипсы отбора, сводка внимания, быстрые действия «назначить» и
  «проверил», обновление дифом каждые 15 с, бейдж непросмотренных в бургере.
- **Главная прораба**: топ сотрудников и переключатель «механики /
  прорабы» открыты прорабу.
- **Снос**: экраны задач обеих ролей с архивами, `MyDrawer`,
  `DrawerForeman`, старая лента сданных — ≈7 900 строк подрядчика.

## Последняя сессия — S08

Лента подключена в оболочки, снесено замещённое. Руками на `make up`:
админ и прораб — все семь пунктов бургера, карточка объекта и «назад»,
«Выйти» с диалогом. Проверки: `flutter test` — 545, `make lint` чист,
`flutter build web` собран.

Найдено и не чинилось: `POST /work/maintenance/{id}/review/` отдаёт прорабу
403 под чипсом «Мои участки»; «FocusScopeNode used after disposed» при
«Выйти» с открытым бургером — шум debug-сборки.

## Записано в базу

- [[раздел кабинета выбирается адресом через Navigator 2.0, а не пакетом go_router]]
- [[бургер один на админа и прораба, а состав разделов задан одним enum]]
- [[заявки и акты ТО идут одной лентой GET work feed, а старые ручки живут deprecated]]
- [[бейдж в бургере кормит блок ленты, а не свой запрос]]
- по ходу эпика через `/note`: [[внимание в ленте работ - три причины с порогами таймера, а не своя шкала]],
  [[лента работ обновляется дифом широким отбором, а не перезапросом]],
  [[панель браузера Claude дёргает видимость вкладки каждые 6 с - resumed не повод опрашивать]],
  [[снятые экраны подрядчика оставляют пустой слот, а не сдвигают индексы оболочки]]
- пожелания заказчика из `inbox/` исполнены и ушли в `archive/inbox/`.

## Дальше

Выбор в `/plan`: E04 «Письмо о назначении» по образцу заказчика
([[приказ о назначении ответственных - образец от заказчика для E04]])
либо хвосты E03 — карточка работы по клику из ленты и снятие
`IntTest.index*` (415 мест в 91 файле).
