---
tags: [session, дефектные акты, карточка работы, лента работ, E03]
date: 2026-09-15
---

# 2026-09-15 - дефекты заявки в карточке работы и карточка влита в main

Задача вне эпиков («Другая задача»), worktree `practical-goldberg-1dbd7c`.
Коммиты в main: `06eaf86` — карточка работы по нажатию на строку ленты
(из предыдущей сессии, ветка `stage-start-5d630e`), `3363d56` — дефекты по
заявке, `23fbd1c` — merge. Ветка удалена, main запушен.

## Что сделано

- **Бэк**: `GET /defective-act/by-order/{order_id}/` по образцу
  `by-act-fact`; тесты своя/чужая/несуществующая заявка; снимок OpenAPI +1
  путь; `make test` — 1049.
- **Фронт**: `DefectsRepository.byOrder`, `WorkDefectsSection(byOrder: true)`
  в ветке `OrderReady` карточки; тест «у заявки есть дефекты, и по строке
  открывается акт»; `flutter test test/works test/schedule` — 195.
- **Превью `works-preview`** (порт 5614): фото на пунктах ТО и в заявке из
  бандла, дефекты по всем видам работ с описаниями, фото и ушедшими клиенту
  актами; `hasDefect` в фикстуре ленты проставлен тем же строкам.

## Записано в базу

- [[дефекты заявки в карточке работы идут своей ручкой by-order, а блок один на ТО и заявку]]

## Замечено рядом, не чинилось

- `flutter run` из worktree сам делает `pub get` — `pubspec.lock` не
  изменился, но в CLAUDE.md сказано его не запускать; лучше копировать
  `frontend/.dart_tool` из основного чекаута до запуска.
- `.test.env` в worktree нет — `make test` падает на `test-db-up`;
  копируется из основного чекаута руками.
- Хвост E03 «строки ленты не нажимаются» закрыт карточкой; снос
  `submitted_works/*`, `in_progress_works/widgets/in_progress_*`,
  `work_card_live` ещё впереди.
