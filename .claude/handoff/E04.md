---
этап: E04·S05 — проверка на собранном стеке и снос «Письма о назначении»
статус: закрыт
дата: 2026-09-15
ветка: E04-S05
план: .claude/plan/E04-pismo-o-naznachenii.md
знание: не записано · эпик E04 закрыт
---

# Передача: эпик E04 закрыт — приказ о назначении принят на стеке, код подрядчика снесён

## Сделано и проверено

- На `make up` (проект `lift_app`, образ из worktree) админ и прораб из карточки
  объекта открывают диалог с живыми ФИО и лифтами и получают PDF в новой
  вкладке того же origin (`/api/v1/static/objects/{id}/appointment_order/….pdf?token=`).
  Прораб смотрел на объекте 82, админ — на 24; PDF принят пользователем глазами.
- Объект без механика (создан тестовый id 90 «S05 тест без механика»): в диалоге
  «Электромеханик не закреплён за объектом», PDF собирается с прочерком.
- Дата: фронт слал секунды местной полуночи, бэк в контейнере (UTC) печатал «14»
  при 15.09 в диалоге. Модель `appointment_order_draft.dart` теперь ↔ полночь UTC,
  как `date_from_timestamp` на бэке. Коммит `f09266e`.
- Выключенный бэк за nginx — это 502 за ~3 с, не обрыв сети; репозиторий теперь
  показывает на 502/503/504 «Не удалось связаться с сервером» (+тест). Тот же коммит.
- Снос «Письма о назначении»: `helper/letter_of_appointment.dart`,
  `foreman/…/letter_of_appointment_foreman.dart`, строка «Документ / Письмо о
  назначении» и загрузка скана в `object_page.dart`, блок у прораба, мёртвые
  комментарии в трёх формах создания объекта и в архивной карточке; в строке
  «Акт» админки имя файла проверяло `letter_of_appointment` → `act_pto`.
  `grep -rn "letter_of_appointment\|LetterOfAppointment" frontend/lib` пуст.
  Коммит `fb232dc`, −1868 строк. Обе карточки после сноса смотрели на стеке.
- Проверки: `dart analyze` по затронутым папкам — 0 ошибок (предупреждения
  подрядчика остались); `flutter test test/object` — 14 зелёных; `flutter build
  web` ✓; `make lint` ✓; `cd backend && uv run pytest` — 1046 passed.

## Не доделано

- `make test` из worktree не запускается: контейнер `lift-test-postgres` держит
  compose-проект основного чекаута. pytest гонялся напрямую на нём — результат тот же.
- 403 на чужом объекте прорабом на стеке недостижим: по `core/access.py` прораб
  читает всё (`ScopeKind.ALL`) — решение заказчика. Ветка покрыта тестом репозитория.
- Ветка `E04-S05` не влита в main; тестовый объект 90 остался в dev-базе.

## Следующий этап

Эпик закрыт. Замечено рядом, вне E04 (для нового эпика или `/note`):

- Карточка объекта админа падает `Null check operator` на объекте без дат ТО /
  договора / контактного лица (создан через API без них) — ищется в
  `screns/object/view/object_page.dart`, не в блоке приказа.
- Блок «Документы» в админской карточке скрыт при ширине окна < 840
  (`object_page.dart`, `if(size.width > 840)`) — кнопки приказа на узком окне нет.
- Поиск по объектам шлёт `?search=`, бэк параметр игнорирует, клиент фильтрует
  только загруженную страницу и не находит объекты со второй.
- Бэк: docstring `format_order_date` в `services/appointment_order_pdf.py`
  говорит про местное время — фактически контейнер UTC; поправить словами.

## Не трогать

- Бэк E04 (`draft`, `pdf`, снимок OpenAPI) — контракт зафиксирован и принят.
- `pubspec.yaml`/`.lock`; `flutter pub get` не запускать.

## Ссылки

- `frontend/lib/screns/object/appointment_order/` — кнопка, диалог, модель, репозиторий (всё живое E04).
- `.claude/plan/E04-pismo-o-naznachenii.md` — план эпика, 5/5.
