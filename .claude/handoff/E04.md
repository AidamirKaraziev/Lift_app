---
этап: E04·S02 — ручка черновика приказа о назначении
статус: закрыт
дата: 2026-09-13
ветка: E04-S02
план: .claude/plan/E04-pismo-o-naznachenii.md
---

# Передача: черновик приказа отдаёт бэк, дальше — PDF на сервере

## Сделано и проверено

- `GET /object/{id}/appointment-order/draft` (`backend/src/api/api_v1/endpoints/object.py`)
  отдаёт `SingleEntityResponse[AppointmentOrderDraft]`: `number:""`, `date`
  (timestamp сегодня), `city:""`, `address`, `organization`, `signer_position`
  («Генеральный директор», если у организации есть `director`), `signer_name`,
  `foreman{full_name, position}`, `mechanic{…}`, `lifts[{type, brand,
  load_capacity_kg}]` — один лифт объекта. Нет связи — `null`/`[]`, не ошибка.
- Схемы `backend/src/schemas/appointment_order.py` (там же константы
  должностей из образца), сборка `backend/src/getters/appointment_order.py`.
- Видимость как у карточки (`crud_objects.get_object_by_id` + `get_raise`):
  прораб читает всё, механик — только своё (403), несуществующий — 404.
  Решение пользователя: оставить как у карточки.
- `backend/tests/test_api_appointment_order.py` — 5 тестов; снимок
  `tests/snapshots/openapi_surface.json` +1 путь.
- `uv run pytest` — 1035 passed; `scripts/lint.sh` чист. Коммит `5d208f5`.

## Не доделано

- Ручка не дёргалась руками на `make up` — только тесты. Проверка на стеке — S05.
- Ветка `E04-S02` не влита в main и не запушена.

## Следующий этап

**Цель.** S03 — `POST /object/{id}/appointment-order/pdf` принимает правленые
реквизиты и отдаёт PDF приказа по образцу заказчика.

**Готово, когда.** Шапка с основанием (ТР ТС 011/2011, ПП РФ № 1744), два
назначения с одним перечнем оборудования, подпись, две строки «ознакомлен»;
кириллица; ссылка через `export-link`; тест на генерацию (критерий подплана).

## Первые шаги

1. Тело запроса = `AppointmentOrderDraft` целиком (фронт отдаёт правленый
   черновик) — переиспользовать схему из `backend/src/schemas/appointment_order.py`.
2. Генерация — reportlab по образцу `backend/src/services/defective_act_pdf.py`
   (шрифт с кириллицей `static/fonts/NotoSans-Regular.ttf`, регистрация там же);
   новый файл `backend/src/services/appointment_order_pdf.py`.
3. Отдача и ссылка — как `POST /defective-act/{id}/pdf/`
   (`backend/src/api/api_v1/endpoints/defective_act.py:285`) и
   `POST /files/export-link` (`endpoints/files.py:97`); потом `make openapi-update`.

## Не трогать

- `helper/letter_of_appointment.dart`, `foreman/…/letter_of_appointment_foreman.dart`,
  строка «Документ / Письмо о назначении» — снос после S05.
- Диалог и модель S01, ручка S02 — менять только под контракт PDF.
- `pubspec.yaml`/`.lock`; `flutter pub get` не запускать.

## Уточнить перед стартом

- PDF хранить файлом на диске (как дефектный акт) или отдавать потоком без
  записи? От этого зависит, нужен ли `export-link` или достаточно токена ссылки.
- Шапка приказа — текст основания брать дословно из образца в vault?

## Ссылки

- `els-vault/knowledge/business/приказ о назначении ответственных - образец от заказчика для E04.md` — текст приказа для PDF.
- `.claude/plan/E04-pismo-o-naznachenii.md` — критерии S03–S05.
- `backend/src/services/defective_act_pdf.py` — готовый reportlab с кириллицей.
